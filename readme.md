# What this

This is rhe backend server made for this [project](https://github.com/Gregory-febvin/react-sprinboot-Doujin-Nexus.git)
This code shouldn't be use as it's pure garbage and work with just hope and dream.

## Instalation
If you really want to use this you need to install JDK version 25 and install/launch the project by placing yourself where is the pom.xml and use this in your terminal:
```
mvn clean spring-boot:run
```

It should download the depedency and launch the project(hopefully).
I can't confirm it work everywhere since it was only tested on ArchLinux
